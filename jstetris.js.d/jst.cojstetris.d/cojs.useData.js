jst.useDataCode = jst.pushModuleCode(function () {
  // Required data:
  // - Whole grid (colornumbers)
  // - Current tris
  // - Tris queue
  // - Game paused
  // - Held tris
  
  usedata = jst.useData = {}'
  usedata.getData = function () {
    
  }
  usedata.setData = function () {
    
  }
}); // End of jst.useData
